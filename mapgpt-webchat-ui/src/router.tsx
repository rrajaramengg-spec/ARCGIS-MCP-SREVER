/**
 * React Router v7 declarative route configuration.
 * Currently routes are defined inline in App.tsx.
 * This file is a placeholder for future route expansion.
 *
 * Future routes can be added here:
 * - /settings → Settings page
 * - /history → Query history
 * - /layers → Layer management
 */

export const ROUTES = {
    HOME: "/",
} as const;
